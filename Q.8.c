#include<stdio.h>
int main()
{
	int decimalnumber;
	printf("Enter a decimal number:");
	scanf("%d",& decimalnumber);
	printf("The decimalnumber in %d in octal format is:%o|n",decimalnumber,decimalnumber);
}